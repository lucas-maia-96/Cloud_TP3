apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: lucasmaia
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 52046
      targetPort: 52046
      nodePort: 52046
  type: NodePort
